@using Microsoft.AspNetCore.Builder
@using Microsoft.Extensions.Options

@inject IOptions<RequestLocalizationOptions> LocOptions

@{
    var cultureItems = LocOptions.Value.SupportedUICultures
                                    .ToList();
}
        

    @*<form class="widget_shopping_cart_content" asp-controller="Language" asp-action="SetLanguage" method="post" >
        <input type="hidden" name="returnUrl" value="@Context.Request.Path "/>*@
        @*<select name="culture" onchange="this.form.submit();">*@
           
           
                @foreach (var item in cultureItems)
                {
    <span class="datas_item" style="justify-content:space-between;float:left" data-id="@item.Name">@item.NativeName.Substring(0, 2).ToUpper()</span>
                    @*<option  class="mini_cart_item" value="@item.Name">@item.NativeName.Substring(0, 2).ToUpper()</option>*@
                }
        @*</select>
    </form>*@
            
  <script>
      $(document).ready(function () {
          $(".datas_item").click(function () {
            var id=  $(this).data("id");
              $.ajax({
                  method: "POST",
                  url: "/Language/SetLanguage/" + id,

              });
          });
         
      });
    
  </script>



          
    
